{
  "serverInfo": {
    "title": "Calypso Multimodal RAG MCP",
    "description": "Multimodal RAG for source-backed AI answers through the Calypso Responses API.",
    "websiteUrl": "https://docs.calypso.so"
  },
  "tools": [
    {
      "name": "calypso-rag-agent",
      "title": "Calypso RAG Agent",
      "description": "Query the Calypso RAG agent for grounded answers over the configured knowledge base. Use `/new` as the prompt to reset the current conversation and start a fresh thread. When `fileIds` are provided, the MCP uses `rag_policy` retrieval semantics for agent-store files.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Your request. Include context, constraints, and desired output."
          },
          "fileIds": {
            "type": "array",
            "description": "Optional uploaded agent-store `file_id` values to attach with retrieval-backed RAG semantics.",
            "items": {
              "type": "string"
            }
          }
        },
        "required": ["prompt"]
      },
      "annotations": {
        "title": "Calypso RAG Agent",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "optional"
      }
    },
    {
      "name": "calypso-list-knowledge-buckets",
      "title": "Calypso List Knowledge Buckets",
      "description": "List all knowledge buckets for the team tied to the configured Calypso API key. Use this before uploads to discover bucket ids, slugs, names, member counts, and bucket-store readiness.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "includeArchived": {
            "type": "boolean",
            "description": "If true, include archived buckets. Defaults to false."
          }
        }
      },
      "annotations": {
        "title": "Calypso List Knowledge Buckets",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "optional"
      }
    },
    {
      "name": "calypso-upload-agent-file",
      "title": "Calypso Upload Agent File",
      "description": "Upload a file into the agent store and return a compatible OpenAI-style `file_id`. This tool sends `purpose=user_data`, targets the selected RAG agent with `target_model`, and can wait until the file is ready for `rag_policy` retrieval.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filename": {
            "type": "string",
            "description": "Display filename for the uploaded file."
          },
          "mimeType": {
            "type": "string",
            "description": "Content type for the uploaded file."
          },
          "contentBase64": {
            "type": "string",
            "description": "Base64-encoded file content. Recommended for Claude, Smithery, browser, and hosted-agent uploads because the MCP process receives the bytes directly."
          },
          "filePath": {
            "type": "string",
            "description": "Advanced local-only path. Use only when the Calypso MCP server runs on the same filesystem and can read this exact path; hosted attachment paths such as /mnt/user-data/uploads usually require contentBase64 instead."
          },
          "targetModel": {
            "type": "string",
            "description": "Optional RAG agent id. Defaults to `calypso-rag-agent`."
          },
          "waitForReady": {
            "type": "boolean",
            "description": "If true, wait until the uploaded file is RAG-ready before returning."
          }
        },
        "required": ["filename", "mimeType"]
      },
      "annotations": {
        "title": "Calypso Upload Agent File",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "execution": {
        "taskSupport": "optional"
      }
    },
    {
      "name": "calypso-upload-knowledge-file",
      "title": "Calypso Upload Knowledge File",
      "description": "Upload a file into the durable bucket-backed knowledge store and indexing pipeline. This tool returns knowledge-file and task metadata and requires bucketIds, bucketSlugs, or bucket.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filename": {
            "type": "string",
            "description": "Display filename for the uploaded knowledge file."
          },
          "mimeType": {
            "type": "string",
            "description": "Content type for the uploaded knowledge file."
          },
          "contentBase64": {
            "type": "string",
            "description": "Base64-encoded file content. Recommended for Claude, Smithery, browser, and hosted-agent uploads because the MCP process receives the bytes directly."
          },
          "filePath": {
            "type": "string",
            "description": "Advanced local-only path. Use only when the Calypso MCP server runs on the same filesystem and can read this exact path; hosted attachment paths such as /mnt/user-data/uploads usually require contentBase64 instead."
          },
          "title": {
            "type": "string",
            "description": "Optional human-readable title stored with the knowledge file."
          },
          "tags": {
            "type": "array",
            "description": "Optional tags for knowledge-store organization.",
            "items": {
              "type": "string"
            }
          },
          "metadata": {
            "type": "object",
            "description": "Optional metadata object serialized with the upload.",
            "additionalProperties": true
          },
          "bucketIds": {
            "type": "array",
            "description": "Existing knowledge bucket ids to assign this upload to. Required unless bucketSlugs or bucket is provided.",
            "items": {
              "type": "string"
            }
          },
          "bucketSlugs": {
            "type": "array",
            "description": "Knowledge bucket slugs to assign this upload to. Required unless bucketIds or bucket is provided.",
            "items": {
              "type": "string"
            }
          },
          "bucket": {
            "type": "string",
            "description": "Convenience single bucket slug for this upload. Required unless bucketIds or bucketSlugs is provided."
          },
          "createMissingBuckets": {
            "type": "boolean",
            "description": "If true, create missing bucket slugs before assignment."
          },
          "idempotencyKey": {
            "type": "string",
            "description": "Optional idempotency key for durable upload retries."
          },
          "waitForIndexing": {
            "type": "boolean",
            "description": "If true, wait until indexing reaches a terminal ready state before returning."
          }
        },
        "required": ["filename", "mimeType"]
      },
      "annotations": {
        "title": "Calypso Upload Knowledge File",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "execution": {
        "taskSupport": "optional"
      }
    },
    {
      "name": "calypso-upload-knowledge-files-batch",
      "title": "Calypso Upload Knowledge Files Batch",
      "description": "Upload 1 to 100 files into the durable knowledge store and indexing queue in one request. A shared bucket destination is required unless every item has its own bucket destination.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "items": {
            "type": "array",
            "description": "Knowledge files to upload in this batch.",
            "minItems": 1,
            "maxItems": 100,
            "items": {
              "type": "object",
              "properties": {
                "filename": {
                  "type": "string",
                  "description": "Display filename for this knowledge file."
                },
                "mimeType": {
                  "type": "string",
                  "description": "Content type for this knowledge file."
                },
                "contentBase64": {
                  "type": "string",
                  "description": "Base64-encoded file content. Recommended for Claude, Smithery, browser, and hosted-agent uploads because the MCP process receives the bytes directly."
                },
                "filePath": {
                  "type": "string",
                  "description": "Advanced local-only path. Use only when the Calypso MCP server runs on the same filesystem and can read this exact path; hosted attachment paths such as /mnt/user-data/uploads usually require contentBase64 instead."
                },
                "clientFileId": {
                  "type": "string",
                  "description": "Optional Firestore-safe id for this batch item. Omit to generate one."
                },
                "title": {
                  "type": "string",
                  "description": "Optional human-readable title for this item."
                },
                "tags": {
                  "type": "array",
                  "description": "Optional tags for this item.",
                  "items": {
                    "type": "string"
                  }
                },
                "metadata": {
                  "type": "object",
                  "description": "Optional metadata object for this item.",
                  "additionalProperties": true
                },
                "bucketIds": {
                  "type": "array",
                  "description": "Existing bucket ids for this item. Required when no shared bucket destination is provided.",
                  "items": {
                    "type": "string"
                  }
                },
                "bucketSlugs": {
                  "type": "array",
                  "description": "Bucket slugs for this item. Required when no shared bucket destination is provided.",
                  "items": {
                    "type": "string"
                  }
                },
                "bucket": {
                  "type": "string",
                  "description": "Convenience single bucket slug for this item. Required when no shared bucket destination is provided."
                },
                "createMissingBuckets": {
                  "type": "boolean",
                  "description": "If true, create missing bucket slugs for this item."
                }
              },
              "required": ["filename", "mimeType"]
            }
          },
          "batchIdempotencyKey": {
            "type": "string",
            "description": "Required idempotency key used to derive the durable batch id."
          },
          "bucketIds": {
            "type": "array",
            "description": "Existing bucket ids applied to all items by default. Required unless every item has a bucket destination.",
            "items": {
              "type": "string"
            }
          },
          "bucketSlugs": {
            "type": "array",
            "description": "Bucket slugs applied to all items by default. Required unless every item has a bucket destination.",
            "items": {
              "type": "string"
            }
          },
          "bucket": {
            "type": "string",
            "description": "Convenience single bucket slug applied to all items by default. Required unless every item has a bucket destination."
          },
          "createMissingBuckets": {
            "type": "boolean",
            "description": "If true, create missing shared bucket slugs before assignment."
          },
          "dryRun": {
            "type": "boolean",
            "description": "If true, validate the batch request without storing files."
          },
          "waitForBatchReady": {
            "type": "boolean",
            "description": "If true, poll batch status with include_items=true until terminal or timeout."
          }
        },
        "required": ["items", "batchIdempotencyKey"]
      },
      "annotations": {
        "title": "Calypso Upload Knowledge Files Batch",
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "execution": {
        "taskSupport": "optional"
      }
    }
  ]
}
